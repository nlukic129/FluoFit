# OS
.DS_Store
**/.DS_Store
*.log

# Agent / skill tooling
# Vendored skill files are local (like node_modules); the lock is the manifest
# (like package-lock.json) and IS tracked so `npx skills add` can restore the set.
.agents/
.claude/
node_modules/
