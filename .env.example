# Copy to .env and fill from the `pnpm db:start` output. Never commit real keys.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=
# Server-only (Edge Functions / engines). BYPASSES RLS — never ship to a client bundle.
SUPABASE_SERVICE_ROLE_KEY=
