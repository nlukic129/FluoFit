// PLACEHOLDER — regenerate from the live schema once Supabase is running locally:
//
//   pnpm db:start        # boots Postgres + applies supabase/migrations
//   pnpm db:types        # → overwrites this file with the generated types
//
// (see package.json scripts). Until then, queries are loosely typed. The schema of record is
// docs/architecture/data-model.md + supabase/migrations, not this file.
export type Database = any;
